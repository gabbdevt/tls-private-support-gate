# tls-private-support-gate
Custom WordPress plugin that restricts access to a private support page by validating users against GoHighLevel CRM tags via WP Fusion. Features HMAC-signed cookie sessions, 15-minute inactivity timeout, Intercom identity verification, and an admin diagnostic panel for real-time tag and access management."
